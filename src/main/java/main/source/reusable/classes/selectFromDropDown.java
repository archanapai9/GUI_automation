package main.source.reusable.classes;

import org.apache.bcel.generic.Select;
import org.openqa.selenium.WebDriver;

public class selectFromDropDown extends Event{
	
	public selectFromDropDown(WebDriver driver) {
		
	}
	
	/*
	 * Given a dropdown check if drop down is visible, check if select text present, select
	 */
	public void actualSelect(Select select, String selectText) {
		
		
	}

}
